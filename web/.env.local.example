# Base URL of the pubmed-evidence-agents FastAPI backend the proxy routes forward to.
# The backend is started with: (from pubmed-evidence-agents/) uvicorn main:app --host 0.0.0.0 --port 8015
PUBMED_EVIDENCE_AGENTS_API_URL=http://localhost:8015
